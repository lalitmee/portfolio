export const config = {
  sections: {
    home: true,
    skills: true,
    quotes: true,
    projects: true,
    certifications: false, // Set to true when data is ready
    blogArticles: false, // Set to true when data is ready
    digitalMarketingProjects: false, // Set to true when data is ready
    testimonials: false, // Set to true when data is ready
    resume: true,
    contact: true,
    footer: true,
  },
  // Other config options can be added here
  darkMode: true, // Enable dark mode toggle
  animations: true, // Enable animations
};
